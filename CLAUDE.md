# line-stamp-note — プロジェクト指示

note で有料販売する教材『【2026年最新版】生成AIでLINEスタンプを作って販売する完全ガイド』のリポジトリ。

## このリポジトリの性質

- **パブリックリポジトリ**（`shimanto/line-stamp-note`）
- 公開前提なので、APIキー・PAT・個人情報・実売上データを一切置かない
- 原稿は note へコピー＆ペーストして使う。Markdown はプレーンに保つ

## ディレクトリ構成

| パス | 内容 |
| --- | --- |
| `manuscript/` | 第0〜13章の原稿 + `full_manuscript.md`（結合版） |
| `prompts/` | ChatGPT / Claude / 画像生成AI 用プロンプト集（7本） |
| `templates/` | 読者がそのまま使える記入用テンプレート（5本） |
| `scripts/` | Python スクリプト（画像検証・リネーム・CSV生成） |
| `assets/screenshots/` | スクリーンショット置き場（未撮影） |
| `assets/sample-images/` | サンプル画像置き場（未作成） |

## 執筆ルール（厳守）

1. **架空の事実を書かない**
   - 具体的な売上金額・販売数・審査日数を創作しない
   - LINE の規約条文を推測で引用しない
2. **変更されうる仕様は断定しない**
   - 画像サイズ・価格帯・分配金・AI利用ルールは「参考値」として扱う
   - 必ず「LINE Creators Market公式の最新ガイドラインを確認してください」を添える
3. **文体**
   - 結論を先に書く / 1文を短く / 2〜4文ごとに改行
   - 上から目線にしない / 不安をあおらない
   - 「簡単に必ず稼げる」等の誇張表現を使わない
4. **権利**
   - 実在人物・既存キャラクター・企業ロゴに似せる指示を書かない
   - 商用利用条件は読者自身が確認する前提で書く

## 見出し階層

- 各章ファイル：`#` は章タイトル1つのみ。以下 `##` → `###`
- `full_manuscript.md` は `scripts/build_full_manuscript.py` で生成する
  （各章の見出しを1段下げ、書籍タイトルを `#` にする。コードフェンス内は変換しない）

## 章を編集したあとの再生成（順番どおりに実行する）

```bash
python scripts/build_full_manuscript.py    # 結合版
python scripts/build_reading_site.py       # 通読版（挿絵つき）
python scripts/build_note_version.py       # note貼り付け用
python scripts/analyze_readability.py      # 読みやすさの実測
```

デプロイ:

```bash
node <wrangler> pages deploy site --project-name line-stamp-note \
  --commit-dirty=true --branch main
```

## note に貼るときの制約（実機で検証済み・2026-07-30）

noteの編集画面に貼り付けて確認した結果。

**保持されるもの**

- 見出し（h2/h3）→ noteの目次が自動生成される
- 箇条書き、番号リスト、太字、引用、区切り線、コードブロック
- 画像（pages.dev の絶対URLから取り込まれる）

**保持されないもの**

- **表**：平文に潰れ、項目の対応関係が読めなくなる
  → `build_note_version.py` が全53個を箇条書きに変換する
- **チェックボックス**：機能がないため `[ ]` が文字で残る
  → `□` に変換する
- **4段目以降の見出し**：noteの見出しは3段階まで
  → 太字の段落に変換する

**注意**

- 貼り付け前に既存本文を消さないと、後ろに追記されて二重になる
- コードブロックは折りたためないため、20行以上には行数の見出しを付ける
- 1記事は5,000〜8,000字が読み切りやすい。9,000字超の章は前後編に分ける
  （`build_note_version.py` の `SPLITS` で設定。14章 → 19記事）

## 推敲について（実測済み・数字で判断する）

`scripts/analyze_readability.py` で測った結果、次はすでに問題ないと確認した。
**感覚で直そうとしないこと。**

- **一文の長さ**：平均20.4字。60字超は全体で8文のみ → 直す必要なし
- **漢字をひらく**：候補は全体で10件、うち数件は誤検出
  （「一致します」「3か所」など）→ **やらない**
- **語尾の偏り**：同じ語尾が3文以上続くのは全体で1箇所のみ → 直す必要なし

読みやすさに効くのは文章ではなく**構造**のほう。

- 記事の長さ（分割）
- 表を箇条書きに開く
- 長いコードブロックの前後に目印を置く

漢字比率が35%前後と出るのは、LINE・画像・申請・審査・透過など
言い換えられない専門語が多いため。ひらくと逆に読みにくくなる。

## 実例キャラクター

教材内の実例は架空キャラクター「アイナ」（AI活用コンサルタント）で統一する。
設定は `manuscript/03_キャラクター設計.md` と `templates/character_sheet.md` が正。
セリフ40個は `manuscript/04_セリフ40個の作り方.md` と `templates/stamp_list.csv` が正。
両方を同時に更新すること。

## Python スクリプトの方針

- 外部API を使わない
- 依存は Pillow のみ
- LINE 側の仕様値はファイル冒頭の設定セクションに集約する（仕様変更時に1か所だけ直す）
- 各関数に日本語コメントと例外処理を入れる
- 元ファイルを上書きしない（`output/` へコピー）
